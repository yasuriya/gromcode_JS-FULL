const array = [1, 2, 3, 4, 5, 6];
const array2 = [7, 8, 9, 10, 11, 12];

function transformToObject(array) {
  const obj = {};

  //   array.map(el => (obj[el] = el));
  //   //   array2.map(el => (obj[el] = el));
  //   //   array.reduce((a, b) => (obj[a] = b));
  //   //   console.log(obj[1]);
  //   console.log(Object.values(obj));
  //   return obj;
}

console.log(transformToObject(array));
